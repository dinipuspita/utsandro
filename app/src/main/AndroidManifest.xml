<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dini_pc.kel3_utsandroid">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".MyDataHelper" />
        <activity android:name=".InsertNovel" />
        <activity android:name=".ViewMovie" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".UpdateNovel" />
        <activity android:name=".MyDataAdapater" />
        <activity android:name=".adapters.MyDataAdapters" />
        <activity android:name=".adapters.RecyclerTouchListener" />
        <activity android:name=".models.RecyclerTouchListener" />
        <activity android:name=".models.MyDataAdapter" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".details" />
        <activity android:name=".list_data" />
    </application>

</manifest>